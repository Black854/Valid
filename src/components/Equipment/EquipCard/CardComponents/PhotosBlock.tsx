import { Table } from "antd"

type PhotosBlockPropsType = {

}

const PhotosBlock: React.FC<PhotosBlockPropsType> = ({ }) => {
    return (
        <Table 
        
        />
    )
}

export default PhotosBlock